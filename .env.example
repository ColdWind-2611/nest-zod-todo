# ─────────────────────────────────────────────────────────────────────────────
# .env.example —— **形状说明书**，不是配置本身。可以提交，也只该提交这个。
#
# 用法：复制成 `.env` 再改成你自己的值（`.env*` 全部被 gitignore，不会进仓库）
#     cp .env.example .env
#
# ── 四个文件的分工 ──
# app.module.ts 里是 envFilePath: ['.env.<环境>', '.env']，**数组靠前的优先**。
#
#   .env                 ★ 机密 + 通用值。**只有开发机上有这个文件**。
#                          生产服务器上不存在它 —— 机密由部署平台注入（见下）。
#   .env.development     开发环境的差异值。当前留空 = 全部走 .env
#   .env.test            测试环境的差异值。当前留空是**故意的**（见下）
#   .env.production      生产环境的差异值。⚠️ 别在这写真实机密（见下）
#
# ── 规矩 ──
#   任何新的环境变量，按这个顺序落地，一步都不能跳：
#     ① src/config/env.schema.ts   ← 启动即校验，缺了就崩
#     ② 本文件                     ← 让人知道要配
#     ③ 代码里才允许读
#   跳过任何一步 = "配置漂移"的唯一入口（本地能跑、别人 clone 下来崩）。
# ─────────────────────────────────────────────────────────────────────────────

# 决定加载哪个 .env.<环境> 文件。缺省 development。
NODE_ENV="development"

# ── 数据库 ───────────────────────────────────────────────────────────────────
# 开发库。Prisma CLI 也读它（prisma7.config.ts 里的 datasource.url）。
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stage2?schema=public"

# 测试库：**只有 test/setup-e2e.ts 读它**，应用本身不碰。
# e2e 首次跑之前要建库：
#   docker exec stage2-postgres createdb -U postgres stage2_test
DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/stage2_test?schema=public"

# ── 服务 ─────────────────────────────────────────────────────────────────────
PORT=3000

# ── 认证 ─────────────────────────────────────────────────────────────────────
# ★ 本项目唯一的真机密。envSchema 强制至少 32 字符。生成：
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# ⚠️ 泄露它的后果：任何人都能签出合法 token 伪造任意用户（含 ADMIN），鉴权归零。
JWT_SECRET="replace-with-your-own-secret"
JWT_EXPIRES_IN="7d"

# bcrypt 成本因子。10 是常用默认；每 +1 计算耗时翻倍。
# envSchema 把范围卡在 4~15（15 是 bcrypt 自身的硬上界）。
BCRYPT_SALT_ROUNDS=10

# ─────────────────────────────────────────────────────────────────────────────
# 为什么 .env.test 是空的？
#
# 因为 e2e 需要的东西，test/setup-e2e.ts 在**建应用之前**就直接写进了 process.env：
#     process.env.DATABASE_URL = process.env.DATABASE_URL_TEST;
#     process.env.NODE_ENV = 'test';
# 而 @nestjs/config **不会覆盖 process.env 里已存在的键** ——
# 所以测试库那条串必定赢，在这里再写一遍 DATABASE_URL 是**无用功**，
# 写了反而让人误以为它在起作用（这是个会骗人的配置）。
#
# 留空还传达了一个信息：测试环境没有任何"额外"的配置差异。
#
# ── 为什么 .env.production 里不该写真实机密 ──
# 就算它被 gitignore 了，它仍然是"躺在磁盘上、会被备份 / docker cp /
# 误 git add -f 带走的东西"。生产机密的正确来源是部署平台：
# Docker secrets、K8s Secret、systemd 的 EnvironmentFile、或编排器设的环境变量。
# 这一点在 Stage 3 的 3.6 / 3.7（Dockerfile + Nginx）会正面撞上。
#
# ── ⚠️ 一个要记住的约定：生产服务器上不能有 .env 文件 ──
# envFilePath 的第二个元素是写死的 '.env'，所以在生产上它会变成
# ['.env.production', '.env']。如果生产机上存在一个开发用的 .env，
# 而 .env.production 又是空的，应用会**静默连上开发库、用开发密钥签发 token**，
# 而且启动一切正常 —— 正是 §3.1.1 说的那种最难排查的故障。
# 这道防线靠的是运维纪律（生产机上不放 .env），不是代码保证。
# ─────────────────────────────────────────────────────────────────────────────
# ── 安全 ─────────────────────────────────────────────────────────────────────
# CORS 白名单：**逗号分隔的 origin 列表**。校验器会把它拆成数组再逐个校验
# （必须是合法 URL），所以 `*` 在这里是**配不通的**，会在启动时直接报错。
#
# 为什么不做 `*`：本项目用 Bearer token 而不是 cookie，把白名单设成
# "谁都能跨域调" 只会让任何一个网页都能拿用户的 token 打你的接口（配合 XSS
# 或 token 泄漏时威力更大）。列出来的是真正要放行的前端地址。
#
# ⚠️ 改这里时注意：逗号两侧的空格会被自动 trim，但**写完别手抖加引号里的空格**。
CORS_ORIGINS="http://localhost:5173,https://your-frontend-domain.com"

# 限流总开关。**必填，没有默认值** —— 它是安全开关，"必须显式表态"优于"猜一个默认值"。
#
# 关掉（"false"）的**正当理由**只有一个：前面已经有一道真的在限流的关卡 ——
#   反代（Nginx 的 limit_req）、WAF、或云厂商的 API 网关。
#   这时候应用内再限一次会变成"两套阈值互相打架"，排查起来很痛苦。
#
# 关掉之后的**危险**，写清楚再决定：
#   POST /auth/login 就是靠应用内这 5 次/60 秒 + 封禁 300 秒挡住暴力破解的。
#   关掉它 = 攻击者可以无限次猜密码，唯一的成本是 bcrypt 的算力。
#   **只有在反代那侧确实配了同等的 login 限流时，才允许关。**
#
# 取值用 z.stringbool() 解析，认这一组写法（大小写不敏感）：
#   真：true / 1 / yes / on / y / enabled
#   假：false / 0 / no / off / n / disabled
# 写法不在这两组里 = **启动直接报错**，不会静默取个默认值。
#
# ⚠️ 别用 `z.coerce.boolean()` 换掉它。那个内部是 `Boolean(v)`，而环境变量
#    永远是字符串 —— `Boolean('false') === true`，配 "false" 反而把限流**打开**。
#    这类"开关反了"的 bug 在生产上极难发现：服务照常跑，只是防护没了。
#    探针见 scratch/probe-stringbool-vs-coerce.ts。
RATE_LIMIT_ENABLED="true"